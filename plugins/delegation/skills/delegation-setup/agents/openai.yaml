interface:
  display_name: "Delegation Setup"
  short_description: "Install and configure cross-device delegation"
  default_prompt: "Use $delegation-setup to install and configure Delegation on this device."
